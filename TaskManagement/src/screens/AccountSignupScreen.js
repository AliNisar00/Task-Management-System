import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const AccountSignup = ( {navigation} ) => {
    return(
        <View>
            <Text>This is the AccountSignupScreen</Text>
        </View>
    )
}

const styles = StyleSheet.create({})

export default AccountSignup
